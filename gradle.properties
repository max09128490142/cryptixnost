# Gradle
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric — versions verified against a working MC 26.2 mod (Krs-Release-1.1.1-mc26.2).
# loom 1.17-SNAPSHOT is the version that supports MC 26.2 (older loom cannot).
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

# Mod info
mod_version=1.0.0
maven_group=com.cryptix
archives_base_name=cryptix
